import { Route } from '@angular/router';
import { ProductCartComponent } from './product-cart/product-cart.component';

export const productCartRoutes: Route[] = [
  { path: '', component: ProductCartComponent },
];
